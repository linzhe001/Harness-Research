interface:
  display_name: "Build Plan"
  short_description: "Codex wrapper for WF7 implementation planning"
  default_prompt: "Use $build-plan after WF6 architecture design to update the roadmap and stable project blueprint."

policy:
  allow_implicit_invocation: false
