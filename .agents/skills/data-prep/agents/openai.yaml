interface:
  display_name: "Data Prep"
  short_description: "Codex wrapper for WF4 data preparation"
  default_prompt: "Use $data-prep to document dataset readiness, paths, and split artifacts."

policy:
  products:
    - chatgpt
  allow_implicit_invocation: false
