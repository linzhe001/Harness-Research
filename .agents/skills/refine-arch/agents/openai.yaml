interface:
  display_name: "Refine Arch"
  short_description: "Codex wrapper for WF6 architecture design"
  default_prompt: "Use $refine-arch after data and baseline evidence to turn the refined idea into a concrete technical spec."

policy:
  allow_implicit_invocation: false
