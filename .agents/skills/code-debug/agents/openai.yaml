interface:
  display_name: "Code Debug"
  short_description: "Implementation code fixes under src/scripts/configs"
  default_prompt: "Use $code-debug to apply the scoped implementation code change and validate it."

policy:
  products:
    - chatgpt
  allow_implicit_invocation: false
