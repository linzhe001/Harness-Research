interface:
  display_name: "Doc Compiler"
  short_description: "Compile docs from auditable evidence chains"
  default_prompt: "Use $doc-compiler to refresh a current document with an evidence chain."

policy:
  products:
    - chatgpt
  allow_implicit_invocation: false
