interface:
  display_name: "Iterate"
  short_description: "Codex wrapper for WF10 iteration loop"
  default_prompt: "Use $iterate to inspect the current iteration and decide the next WF10 action."

policy:
  products:
    - chatgpt
  allow_implicit_invocation: false
